"""NBA gravity pipeline.

Stage scripts are the numbered modules (`01_detect.py` ... `06_aggregate.py`),
run from the command line. Shared helpers live in the importable modules
alongside them (`common.py`, `shot_boundaries.py`, `detector.py`).
"""
